# ⚠️ ARCHIVED — Legacy React Native (Expo) prototype

This is the **original React Native / Expo** OpsFlow mobile prototype. It is **no longer the active frontend** and is kept only for historical reference (screen designs, primitives, mock data).

**Do not build on this.** The active frontend is the Nx / Angular monorepo at [`/frontend`](../../frontend):
- `frontend/apps/dashboard` — back-office web app
- `frontend/apps/field-pwa` — field / kiosk PWA

Archived 2026-06-28 (moved from the repo root `opsflow-app/`). History preserved via `git mv`.
